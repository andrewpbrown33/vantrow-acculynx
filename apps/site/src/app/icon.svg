<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <!--
    Eaverow favicon: the "Sheltered Dot" mark (light variant — the default)
    on a paper tile so it stays legible on dark browser tab bars.
    Hexes are hardcoded by necessity — no CSS variables in favicon context;
    keep in sync with packages/brand/src/brand.config.ts and
    packages/brand/src/components.tsx (LogoMark geometry).
  -->
  <rect width="100" height="100" rx="22" fill="#f7f6f2"/>
  <path d="M24 66 L50 30 L76 66" fill="none" stroke="#1d4b38" stroke-width="13" stroke-linecap="round" stroke-linejoin="round"/>
  <circle cx="50" cy="62" r="10.5" fill="#b8956a"/>
</svg>
